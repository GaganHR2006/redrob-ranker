# Runtime dependencies (ranking step — stdlib only, no installs needed for rank/)
# Optional: install these for the demo app and dev tools

# Demo app
streamlit>=1.38.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0

# Validation
# validate_submission.py uses only stdlib (csv, pathlib, sys)

# Dev tools
tqdm>=4.65.0
altair>=5.0.0

# Document parsing (for jd_decompose.py pre-computation, not needed for ranking)
python-docx>=1.1.2

# PDF deck generation
fpdf2>=2.7.0
